# Copy this file to `.env.local` and fill in real values.
# `.env.local` is gitignored — NEVER commit real secrets, and never paste them into chat.

# --- xAI / Grok ---
XAI_API_KEY=

# --- Grok Voice agent (fill in when received) ---
# XAI_VOICE_API_KEY=
# XAI_VOICE_ENDPOINT=
# XAI_VOICE_AGENT_ID=

# --- Hx Hub / app config ---
# HX_HUB_URL=
